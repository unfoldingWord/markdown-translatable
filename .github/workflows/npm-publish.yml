name: Publish to npm

# Publishes markdown-translatable to npm when a GitHub release is published.
# Uses an npm AUTOMATION token (secrets.NPM_TOKEN, an unfoldingWord org secret)
# which is exempt from the org's 2FA-on-write requirement, so no interactive
# OTP is needed.
#
# There is no explicit build step: dist/ is gitignored and package.json's
# `prepack` script babel-compiles src/ -> dist/, which npm runs automatically
# as part of `npm publish`.
#
# Idempotent: if the version in package.json is already on the registry, the
# publish step is skipped (so re-running a release workflow passes green
# instead of erroring).
#
# Create a release tagged vX.Y.Z (matching package.json's version) to trigger it.
on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
          cache: yarn

      - name: Install
        run: yarn install --frozen-lockfile

      - name: Test
        run: yarn test

      - name: Verify release tag matches package.json version
        if: github.event_name == 'release'
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          PKG="$(node -p "require('./package.json').version")"
          if [ "$TAG" != "$PKG" ]; then
            echo "::error::Release tag ($TAG) does not match package.json version ($PKG)."
            exit 1
          fi

      - name: Publish to npm (skip if version already published)
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          NAME="$(node -p "require('./package.json').name")"
          VER="$(node -p "require('./package.json').version")"
          if npm view "$NAME@$VER" version >/dev/null 2>&1; then
            echo "$NAME@$VER is already on the registry — skipping publish."
          else
            # prepack builds dist/ via babel; the package is unscoped so it
            # publishes public by default.
            npm publish
          fi
